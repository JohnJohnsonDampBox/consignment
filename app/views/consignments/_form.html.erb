<%= form_for(@consignment) do |f| %>
  <% if @consignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consignment.errors.count, "error") %> prohibited this consignment from being saved:</h2>

      <ul>
      <% @consignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :owner_id, Owner.all.collect { |o| [o.name, o.id] } %><br>
  </div>
  <div class="field">
    <%= f.select :good_id, Good.all.collect { |g| [g.name, g.id] } %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
